请使用cmake和vcpkg安装适当的库并完成构建的CMakeFileLists.txt等程序测试完成并完成README.md之后才能完成任务，题目如下，vcpkg和cmake都是已经安装在系统上的不需要再进行安装，直接完成任务即可，并且完成.gitignore和git init，使用C/C++完成
并且如果编译失败可以尝试读取$env:VCPKG_ROOT，然后默认的位置$HOME\vcpkg在这个位置
必须完成所有任务之后才能结束任务，另外如果有任何环境不清楚的马上询问用户
在所有任务完成后执行这些指令推送到git：
git init && git add . && git commit -m "Initial commit" && git remote add origin {问问用户git地址} && git push -u origin master
请完成任务：